// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
export class Session extends APIResource {
    /**
     * Create a new browser session on our distributed network.
     *
     * @example
     * ```ts
     * const session = await client.browser.session.create();
     * ```
     */
    create(body = {}, options) {
        return this._client.post('/v1/browser/session', { body, ...options });
    }
    /**
     * List browser sessions with pagination.
     *
     * @example
     * ```ts
     * const sessions = await client.browser.session.list();
     * ```
     */
    list(query = {}, options) {
        return this._client.get('/v1/browser/sessions', { query, ...options });
    }
    /**
     * Retrieve an existing browser session.
     *
     * @example
     * ```ts
     * const session = await client.browser.session.get({
     *   sessionId: 'example-session-id',
     * });
     * ```
     */
    get(query, options) {
        return this._client.get('/v1/browser/session', { query, ...options });
    }
    /**
     * Stop an existing browser session.
     *
     * @example
     * ```ts
     * const response = await client.browser.session.stop({
     *   sessionId: 'example-session-id',
     * });
     * ```
     */
    stop(params, options) {
        const { sessionId } = params;
        return this._client.delete('/v1/browser/session', { query: { sessionId }, ...options });
    }
}
//# sourceMappingURL=session.mjs.map